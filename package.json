{
  "name": "invoice-generator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38",
    "pdfmake": "^0.2.20"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
